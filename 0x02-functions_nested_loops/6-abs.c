#include "main.h"
/**
 * _abs -  function
 * @x: used to get inputs
 * Description: used to print absolute value of an integer.
 * Return: 0 for now
 */
int _abs(int x)
{
if (x < 0)
{
return (-x);
}
else
{
return (x);
}
}
