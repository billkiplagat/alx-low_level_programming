/**
 * _isdigit - function
 * @c: to get inputs
 * Description: this function checks if a char is a digit
 * Return: 1 if true 0 else
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
else
return (0);
}
