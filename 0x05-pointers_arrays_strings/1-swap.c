/**
 * swap_int - function to swap numbers
 * @a: get first number
 * @b: get second number
 * Description: the function swaps 2 numbers
 */
void swap_int(int *a, int *b)
{
int temp = *a;
*a = *b;
*b = temp;
}
