#include "main.h"
/**
 * _strlen - function
 * @s: to get char input
 * Description: prints the number of char in a string
 * Return: length
 */
int _strlen(char *s)
{
int length = 0;
for ( ; s[length] != '\0'; length++)
{
}
return (length);
}
